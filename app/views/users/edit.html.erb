<h1>Edit User</h1>

<% simple_form_for @user, :html => { :multipart => true } do |f| %>
	<fieldset>
		<%= render 'form', :f => f %>
		<%= f.button :submit, 'Update profile' %>
	</fieldset>
<% end %>

<p><%= link_to '← Back', :back %></p>
