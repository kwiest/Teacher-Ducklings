<%= f.error_messages %>

<p>
	<%= f.label :video_id %>
	<%= f.collection_select :video_id, Video.find(:all), :id, :title %>
</p>
<p>
	<%= f.label :description, "Description:" %>
	<%= f.text_area :description, :rows => 5 %>
</p>
<p>
	<%= f.label :pdf, "Attach a PDF:" %>
	<%= f.file_field :pdf %>
</p>
<p>
	<%= f.label "" %>
	<%= f.submit "Add a Review" %>
</p>